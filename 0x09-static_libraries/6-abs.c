#include "main.h"

/**
 * _abs - checks the absolute value of an integer
 * @n: parameter to be checked
 * Return: 0
 */

int _abs(int n)
{
	if (n >= 0)
	{
		return (n);
	}
	else
	{
		return (-n);
	}
	return (0);
}
