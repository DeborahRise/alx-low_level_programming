#include "main.h"
#include <stdio.h>
#include <string.h>

/**
 * *_strcpy - copies 2 strings
 * @dest: first pointer
 * @src: second parameter
 * Return: 0
 */

char *_strcpy(char *dest, char *src)
{
	strcpy(dest, src);
		return (dest);
}
